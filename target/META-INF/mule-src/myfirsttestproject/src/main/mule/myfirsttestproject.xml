<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
      http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
      http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
      http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <http:listener-config name="HTTP_Listener_config" doc:name="HTTP Listener config">
        <http:listener-connection host="localhost" port="8081"/>
    </http:listener-config>

    <!-- Single flow with all logic -->
    <flow name="mainFlow">
        <http:listener path="/orders" config-ref="HTTP_Listener_config" doc:name="listener"/>

        <!-- Hardcoded database connection string -->
        <set-variable value="jdbc:mysql://localhost:3306/badDB" doc:name="Set DB String" variableName="dbString"/>

        <!-- Hardcoded JSON string as input -->
        <set-payload value="{ 'id': '123', 'name': 'bad order', 'amount': 500 }"/>

        <!-- Terrible DataWeave -->
        <ee:transform doc:name="Transform Message" doc:id="693f588f-546e-4bb6-89df-60ffefcce177" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/java
---
{
}]]></ee:set-payload>
			</ee:message>
		</ee:transform>
		<transform doc:name="transform">
            <ee:transform>
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload]]></ee:set-payload>
                </ee:message>
            </ee:transform>
        </transform>

        <!-- No error handling, no logging -->

        <!-- Dummy call to simulate external system -->
        <http:request method="POST" url="http://someapi.com/post" doc:name="External call">
            <http:request-builder>
                <http:header headerName="Authorization" value="Bearer HARDCODED_TOKEN"/>
            </http:request-builder>
        </http:request>

        <!-- Return response -->
        <set-payload value="Order Processed"/>
    </flow>

</mule>
